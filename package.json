{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "pnpm start --prefix packages/client/",
    "serve": "pnpm start --prefix packages/server/",
    "dev": "pnpm recursive run dev --filter ./packages/server --filter ./packages/client",
    "dev:client": "pnpm recursive run dev --filter ./client",
    "dev:server": "pnpm recursive run dev --filter ./server"
  }
}
